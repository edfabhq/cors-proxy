services:
  caddy:
    image: caddy:2.11-alpine
    ports:
      - "${PORT}:8080"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
    environment:
      - ALLOWED_HOSTS_REGEX=${ALLOWED_HOSTS_REGEX}
